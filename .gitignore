# ── Rust / Cargo ──────────────────────────────
# 빌드 산출물. 워크스페이스는 target/을 루트에서 공유하므로 이 한 줄이면 전체 커버.
/target

# 오래된 rustfmt 백업 파일
**/*.rs.bk

# ── macOS ─────────────────────────────────────
.DS_Store

# ── 에디터 / IDE ──────────────────────────────
.vscode/
.idea/

# ── 환경변수 (M2 이후 인증 토큰 등 생기면) ────
.env